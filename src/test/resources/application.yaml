spring:
  datasource:
    url: jdbc:h2:mem:duty_checker_test
    driver-class-name: org.h2.Driver
    username: sa
    password:
  h2:
    console:
      enabled: false
  jpa:
    hibernate:
      ddl-auto: create-drop
  data:
    redis:
      host: localhost
      port: 6379

jwt:
  secret: test-secret-key-must-be-at-least-256-bits-long-for-hs256
  access-expiration-ms: 3600000
  refresh-expiration-ms: 7776000000

coolsms:
  api:
    key: test-api-key
    secret:
      key: test-api-secret-key
    domain: https://api.coolsms.co.kr
    from-num: 01000000000
